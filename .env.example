# HuggingFace Token (required for Llama 3.2 1B)
# Get token: https://huggingface.co/settings/tokens
# Accept license: https://huggingface.co/meta-llama/Llama-3.2-1B
HF_TOKEN=your_token_here

# ============================================================================
# Usage Instructions
# ============================================================================

# Local Development:
# 1. Copy this file to .env
# 2. Replace 'your_token_here' with your actual HuggingFace token
# 3. The backend will automatically load .env via python-dotenv
# 4. Start backend: cd backend && uvicorn main:app --reload

# Docker Build (with Llama):
#   docker build --build-arg HF_TOKEN=your_token_here -t ai-fun-token-wheel .

# Docker Run (with Llama):
#   docker run -e HF_TOKEN=your_token_here -p 8080:8080 ai-fun-token-wheel

# Without HF_TOKEN:
#   - GPT-2 only mode (still works!)
#   - Llama 3.2 1B will show as unavailable in the UI
#   - No token required for GPT-2

# GitHub Actions:
#   - Set HF_TOKEN as a repository secret
#   - Workflow will automatically use it during Docker build
